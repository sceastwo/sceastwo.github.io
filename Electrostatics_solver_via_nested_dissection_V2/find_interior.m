
function [interior_nodes, boundary_nodes] = find_interior(master_sparse_matrix, conditioned_node_set, current_node_set)
%FIND_INTERIOR 
%   master_sparse_matrix : is the master matrix that defines the
%   connections between the nodes. 
%   conditioned_node_set : the set of nodes that are conditioned variables.
%   current_node_set : the set of nodes for which the boundary and interior
%   is being computed. 
    
    interior_nodes = [];
    boundary_nodes = [];
    
    
    
end

