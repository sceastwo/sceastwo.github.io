
%%%%%%% structure dense labeled matrix: 
%%%% field "row_labels": a vector of node names that correspond to each row. 
%%%% field "column_labels": a vector of node names that correspond to each
%%%% column. 
%%%% field "M": the matrix whose rows are labeled with the row labels and
%%%% whose columns are labeled with the column labels. 


%%%%%%% structure sparse labeled matrix: 
%%%% field "row_labels": a vector of node names that correspond to each row. 
%%%% field "column_labels": a vector of node names that correspond to each
%%%% column. 
%%%% field "M": the sparse matrix whose rows are labeled with the row labels and
%%%% whose columns are labeled with the column labels. 


